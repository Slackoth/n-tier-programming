@model IEnumerable<Library.Models.biblioteca>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.libro.nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.usuario.contrasena)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.libro.nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.usuario.contrasena)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idbiblioteca }) |
            @Html.ActionLink("Details", "Details", new { id=item.idbiblioteca }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idbiblioteca })
        </td>
    </tr>
}

</table>
